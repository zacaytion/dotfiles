#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#
# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/zprezto/init.zsh"
fi


alias dtop='lazydocker'
alias ec='emacsclient -nc'
alias et='emacsclient -t'

eval "$(starship init zsh)"
export PATH="/usr/local/opt/grep/libexec/gnubin:$PATH"

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/zah/.config/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/zah/.config/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/zah/.config/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/zah/.config/google-cloud-sdk/completion.zsh.inc'; fi

. /usr/local/opt/asdf/libexec/asdf.sh
source "${XDG_CONFIG_HOME:-$HOME/.config}/asdf-direnv/zshrc"

source /Users/zah/.config/broot/launcher/bash/br

# DOTFILES

## File Structure

```sh
/bin    # dotfile specific executables
/pkgs
  /macOS
    /bin           # package specific scripts
    /config.       # pacakge specific configs
    env            # package specific environment variables
    pkg.mk         # package specific Makefile
    Brewfile       # package dependencies
    install.sh     # scripts used for in package install
    README.md

```
